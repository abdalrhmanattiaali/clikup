# 🔍 دليل تشخيص حالات المهام المكتملة

## 🚨 إذا لم تعمل الإشعارات، اتبع هذه الخطوات:

---

## الخطوة 1: أعد تشغيل النظام بالتحديثات الجديدة

```bash
# أوقف النظام الحالي
pm2 stop clikup-bot
# أو اضغط Ctrl+C

# شغّل النظام من جديد
pm2 restart clikup-bot
# أو
npm start
```

---

## الخطوة 2: اختبر حالة "COMPLETE"

افتح في المتصفح:
```
http://localhost:5014/debug/status-check/COMPLETE
```

**النتيجة المتوقعة:**
```json
{
  "originalStatus": "COMPLETE",
  "lowercaseStatus": "complete",
  "isComplete": true,
  "supportedStatuses": [
    "complete",
    "completed",
    "done",
    "finished",
    "closed",
    "canceled",
    "cancelled",
    "complete & not invoiced",
    "مكتمل",
    "منتهي",
    "مغلق",
    "ملغي"
  ],
  "message": "✅ هذه الحالة مكتملة"
}
```

**إذا كانت النتيجة `"isComplete": false`** → الحالة غير موجودة في القائمة

---

## الخطوة 3: أكمل مهمة في ClickUp وراقب السجلات

### افتح السجلات:
```bash
# إذا كنت تستخدم pm2
pm2 logs clikup-bot --lines 100

# أو إذا تشغيل عادي
# راقب الطرفية مباشرة
```

### أكمل مهمة في ClickUp
غيّر حالة أي مهمة إلى "Complete"

### ما يجب أن تراه في السجلات:
```
📊 Status Change Detected:
   Before: In Progress
   After: COMPLETE
   After (lowercase): complete
   Is Complete?: true
✅ Task marked as COMPLETE - sending notification
✅ Creating completion notification for task: "اسم المهمة" (ID: 123), completed by احمد
📤 Message sent to 120363420647183410@g.us
```

---

## الخطوة 4: تشخيص المشكلة

### السيناريو 1: لا يظهر "📊 Status Change Detected" في السجلات

**المشكلة:** ClickUp لا يرسل webhook أصلاً

**الحل:**
1. تحقق من إعدادات Webhook في ClickUp:
   - اذهب إلى: ClickUp Settings → Integrations → Webhooks
   - تأكد من وجود Webhook نشط
   - URL: `http://[السيرفر]:5014/webhooks/clickup`
   - Events: يجب اختيار "Task Updated"

2. تأكد من أن السيرفر يستقبل الطلبات:
```bash
curl http://localhost:5014/api/dashboard-stats
# يجب أن تعمل
```

---

### السيناريو 2: يظهر "📊 Status Change" لكن "Is Complete?: false"

**المشكلة:** اسم الحالة في ClickUp مختلف عن القائمة

**مثال من السجلات:**
```
📊 Status Change Detected:
   Before: In Progress
   After: Completed Task      ← هذا اسم مختلف!
   After (lowercase): completed task
   Is Complete?: false        ← ليست في القائمة!
```

**الحل:**
1. انسخ الاسم الدقيق من السجلات (مثلاً: `completed task`)
2. افتح ملف `index.js`
3. ابحث عن `NON_OPEN_STATUSES` (السطر 31 تقريباً)
4. أضف الاسم الجديد:

```javascript
const NON_OPEN_STATUSES = [
    'complete',
    'completed',
    'completed task',  // ← أضف الحالة الجديدة هنا
    'done',
    // ...
];
```

5. احفظ الملف وأعد التشغيل:
```bash
pm2 restart clikup-bot
```

---

### السيناريو 3: "Is Complete?: true" لكن لا تصل رسالة

**المشكلة:** مشكلة في إرسال الرسالة للواتساب

**الحل:**
1. تحقق من سجلات الخطأ:
```bash
pm2 logs clikup-bot | grep -i error
```

2. تأكد من اتصال WhatsApp:
```bash
# يجب أن ترى في السجلات:
✅ WhatsApp Ready
📢 Group chat found: Click Up notification 📢
```

3. إذا لم يكن WhatsApp متصل:
```bash
# احذف المصادقة وأعد المسح
rm -rf .wwebjs_auth .wwebjs_cache
pm2 restart clikup-bot
# امسح QR Code من جديد
```

---

### السيناريو 4: كل شيء صحيح لكن لا يحدث شيء

**المشكلة:** قد يكون webhook لا يصل أصلاً

**اختبار Webhook:**
```bash
# أرسل webhook يدوياً للاختبار
curl -X POST http://localhost:5014/webhooks/clickup \
  -H "Content-Type: application/json" \
  -d '{
    "event": "taskUpdated",
    "task_id": "test123",
    "history_items": [{
      "field": "status",
      "before": {"status": "In Progress"},
      "after": {"status": "Complete"},
      "user": {"username": "احمد"}
    }]
  }'
```

---

## الخطوة 5: اختبار يدوي مباشر

### اختبر endpoint الاختبار:
```bash
curl http://localhost:5014/debug/status-check/COMPLETE
curl http://localhost:5014/debug/status-check/complete
curl http://localhost:5014/debug/status-check/Done
curl http://localhost:5014/debug/status-check/finished
```

**كل هذه يجب أن تعطي `"isComplete": true`**

---

## الخطوة 6: معرفة الاسم الدقيق للحالة من ClickUp

### الطريقة 1: من السجلات (الأسهل)
1. أكمل مهمة في ClickUp
2. راقب السجلات
3. ابحث عن: `After: ...`
4. هذا هو الاسم الدقيق

### الطريقة 2: من API مباشرة
```bash
# اجلب معلومات مهمة معينة
curl "https://api.clickup.com/api/v2/task/TASK_ID?custom_task_ids=true&team_id=9015343430" \
  -H "Authorization: pk_62585187_VZCCTKCU9501T8G8KJHVGT9FSXPVTU11" \
  | jq '.status'
```

النتيجة ستعرض:
```json
{
  "status": "COMPLETE",
  "color": "#6bc950",
  "type": "done",
  "orderindex": 2
}
```

**الحالة الفعلية:** `COMPLETE`

---

## الخطوة 7: قائمة الحالات المدعومة حالياً

### عرض القائمة:
```bash
curl http://localhost:5014/debug/status-check/test | jq '.supportedStatuses'
```

**النتيجة:**
```json
[
  "complete",
  "completed",
  "done",
  "finished",
  "closed",
  "canceled",
  "cancelled",
  "complete & not invoiced",
  "مكتمل",
  "منتهي",
  "مغلق",
  "ملغي"
]
```

---

## 🔧 الحلول السريعة

### الحل 1: إضافة حالة جديدة
```bash
# 1. افتح الملف
nano index.js

# 2. ابحث عن NON_OPEN_STATUSES (السطر 31)

# 3. أضف الحالة الجديدة
const NON_OPEN_STATUSES = [
    'complete',
    'your-new-status-here',  // ← هنا
    // ...
];

# 4. احفظ: Ctrl+O ثم Enter
# 5. أغلق: Ctrl+X

# 6. أعد التشغيل
pm2 restart clikup-bot
```

### الحل 2: إعادة مسح QR Code
```bash
rm -rf .wwebjs_auth .wwebjs_cache
pm2 restart clikup-bot
# امسح الـ QR Code الجديد
```

### الحل 3: التحقق من Webhook في ClickUp
1. اذهب إلى: https://app.clickup.com/
2. Settings → Integrations → Webhooks
3. تأكد من:
   - ✅ Webhook نشط (Active)
   - ✅ URL صحيح
   - ✅ Events تحتوي على "Task Updated"

---

## 📊 الأوامر المفيدة

```bash
# عرض آخر 50 سطر من السجلات
pm2 logs clikup-bot --lines 50

# متابعة السجلات مباشرة
pm2 logs clikup-bot --follow

# البحث عن أخطاء
pm2 logs clikup-bot | grep -i error

# البحث عن حالات المهام
pm2 logs clikup-bot | grep "Status Change"

# اختبار حالة معينة
curl http://localhost:5014/debug/status-check/COMPLETE

# إعادة تشغيل النظام
pm2 restart clikup-bot

# عرض حالة النظام
pm2 status
```

---

## 🎯 Checklist التشخيص

قبل أن تسأل عن المساعدة، تأكد من:

- [ ] النظام شغال: `pm2 status` يعرض "online"
- [ ] WhatsApp متصل: ترى "✅ WhatsApp Ready" في السجلات
- [ ] Webhook مُعد في ClickUp بشكل صحيح
- [ ] URL الـ Webhook صحيح ويصل للسيرفر
- [ ] اسم الحالة موجود في `NON_OPEN_STATUSES`
- [ ] رأيت "📊 Status Change Detected" في السجلات
- [ ] `Is Complete?` يعطي `true`
- [ ] ترى "✅ Task marked as COMPLETE" في السجلات

---

## 🆘 لا يزال لا يعمل؟

**أرسل هذه المعلومات:**

1. **آخر 50 سطر من السجلات:**
```bash
pm2 logs clikup-bot --lines 50
```

2. **نتيجة اختبار الحالة:**
```bash
curl http://localhost:5014/debug/status-check/COMPLETE
```

3. **الحالة الفعلية من ClickUp:**
   - صورة من اسم الحالة في ClickUp
   - أو نتيجة السجلات عند إكمال مهمة

4. **إعدادات Webhook:**
   - صورة من إعدادات Webhook في ClickUp

---

## ✅ نصائح مهمة

1. **استخدم الحروف الصغيرة دائماً** في `NON_OPEN_STATUSES`
   - ❌ خطأ: `'COMPLETE'`
   - ✅ صحيح: `'complete'`
   - السبب: الكود يحول إلى lowercase تلقائياً

2. **لا تنسَ المسافات**
   - إذا كانت الحالة `'complete & not invoiced'`
   - يجب أن تكون كذلك بالضبط في القائمة

3. **راقب السجلات دائماً**
   - هي المفتاح لمعرفة ما يحدث بالضبط

4. **اختبر بعد كل تغيير**
   - لا تغيّر أشياء كثيرة مرة واحدة
   - غيّر، اختبر، تأكد، ثم غيّر الشيء التالي

---

**🎉 بالتوفيق! معظم المشاكل تُحل من الخطوات أعلاه**
