# 🧪 اختبار Webhook - دليل سريع

## ✅ الحل الفوري

أنت قلت: **"عندما أعمل اختبار يفشل"**

المشكلة: **webhook endpoint موجود** لكنه يقبل **POST فقط** مش GET!

---

## 🎯 الحل السريع (دقيقة واحدة)

### الطريقة 1: اختبار من المتصفح (GET)

افتح المتصفح وروح على:
```
http://147.93.94.125:5014/webhook-test
```

**يجب أن ترى:**
```json
{
  "status": "ok",
  "message": "✅ السيرفر شغّال والـ webhook endpoints جاهزة!",
  "endpoints": { ... },
  "server_info": { ... }
}
```

✅ **لو شفت هذا = السيرفر شغّال 100%**

---

### الطريقة 2: اختبار تلقائي (Script)

على السيرفر:
```bash
./test-webhook-endpoints.sh
```

هيعمل 3 اختبارات:
1. ✅ هل السيرفر شغّال؟
2. ✅ هل webhook endpoint موجود؟
3. ✅ هل يقبل POST requests؟

---

## 🔍 فهم المشكلة

### webhook endpoint في index.js:

```javascript
// ❌ مش هيشتغل - GET
app.get('/task-updated-webhook', ...)   // معلومات فقط

// ✅ هيشتغل - POST
app.post('/task-updated-webhook', ...)  // يستقبل من ClickUp
```

### يعني:
- ❌ لو فتحت المتصفح: `http://147.93.94.125:5014/task-updated-webhook`
  - **هيشتغل لكن هيعطيك معلومات فقط** (مش هيعالج webhook)

- ✅ لو ClickUp بعت POST request:
  - **هيستقبل ويعالج التحديث**

---

## 📍 Endpoints الجديدة للاختبار

### 1. `/webhook-test` (GET)
**اختبار بسيط جداً:**
```bash
curl http://147.93.94.125:5014/webhook-test
```

**النتيجة:**
```json
{
  "status": "ok",
  "message": "✅ السيرفر شغّال والـ webhook endpoints جاهزة!",
  "server_info": {
    "port": 5014,
    "whatsapp_connected": true,
    "group_id": "120363..."
  }
}
```

---

### 2. `/task-updated-webhook` (GET)
**معلومات عن webhook:**
```bash
curl http://147.93.94.125:5014/task-updated-webhook
```

**النتيجة:**
```json
{
  "endpoint": "POST /task-updated-webhook",
  "description": "نقطة وصول webhook لاستقبال تحديثات المهام من ClickUp",
  "method": "POST",
  "status": "✅ جاهز",
  "server_status": {
    "running": true,
    "whatsapp_connected": true,
    "ai_enabled": true
  }
}
```

---

### 3. `/task-updated-webhook` (POST)
**استقبال من ClickUp:**
```bash
curl -X POST http://147.93.94.125:5014/task-updated-webhook \
  -H "Content-Type: application/json" \
  -d '{
    "event": "taskUpdated",
    "task_id": "abc123",
    "history_items": [...]
  }'
```

**النتيجة:** يعالج التحديث ويرسل إشعار

---

## 🎯 خطوات التشخيص الكاملة

### الخطوة 1: تحقق السيرفر شغّال
```bash
# على السيرفر
pm2 status
```

يجب أن ترى `clikup-bot` و `online`

---

### الخطوة 2: اختبر من المتصفح
افتح:
```
http://147.93.94.125:5014/webhook-test
```

**لو شفت JSON:**
- ✅ السيرفر شغّال
- ✅ البورت 5014 مفتوح
- ✅ Endpoints جاهزة

**لو مش شغّال:**
- ❌ السيرفر مش شغّال أو البورت مقفول

---

### الخطوة 3: اختبر webhook info
افتح:
```
http://147.93.94.125:5014/task-updated-webhook
```

**لو شفت معلومات webhook:**
- ✅ Endpoint موجود
- ✅ جاهز لاستقبال POST من ClickUp

---

### الخطوة 4: اختبر POST request
```bash
./test-webhook-endpoints.sh
```

أو يدوياً:
```bash
curl -X POST http://147.93.94.125:5014/task-updated-webhook \
  -H "Content-Type: application/json" \
  -d '{"task_id":"test","history_items":[]}'
```

**النتيجة المتوقعة:**
- HTTP 404 (task not found) ← **ممتاز! معناه endpoint شغّال**
- HTTP 200 (ok) ← **رائع! تم معالجة الطلب**
- HTTP 500 (error) ← **طبيعي إذا البيانات مش كاملة**

---

## 🔧 إعداد ClickUp Webhook

الآن endpoint موجود وشغّال، روح على ClickUp:

### 1. افتح ClickUp
Settings → Integrations → Webhooks

### 2. أضف/عدّل Webhook
```
URL: http://147.93.94.125:5014/task-updated-webhook
Events: ✓ taskUpdated
Method: POST
Status: Active
```

### 3. احفظ

### 4. اختبر
غيّر حالة أي مهمة في ClickUp

### 5. تحقق من السجلات
```bash
pm2 logs clikup-bot --follow
```

يجب أن ترى:
```
📊 Task Update Detected
🤖 Sending to AI...
✅ AI notification sent successfully
```

---

## 🎉 الخلاصة

### المشكلة الأصلية:
- ❌ endpoint موجود لكن GET مش POST
- ❌ اختبار من المتصفح ما كان يشتغل

### الحل:
- ✅ أضفت GET endpoints للاختبار
- ✅ POST endpoint موجود ويشتغل
- ✅ يمكن اختبار من المتصفح الآن

### للتحقق:
```bash
# اختبار بسيط
curl http://147.93.94.125:5014/webhook-test

# اختبار شامل
./test-webhook-endpoints.sh

# مراقبة السجلات
pm2 logs clikup-bot --follow
```

---

## 🚀 الخطوة التالية

1. ✅ تأكد السيرفر شغّال: `pm2 status`
2. ✅ اختبر من المتصفح: افتح `/webhook-test`
3. ✅ عدّل webhook في ClickUp
4. ✅ غيّر حالة مهمة
5. ✅ شوف الإشعار في WhatsApp!

**webhook endpoints موجودة وجاهزة! 🎯**
