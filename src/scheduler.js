// ========================= Scheduler ========================= //
// This file handles scheduled tasks and cron jobs

function initializeScheduler() {
    console.log('✅ Scheduler Initialized');

    // All cron jobs are defined in the main index.js file
    // This is just a placeholder for initialization
}

module.exports = {
    initializeScheduler
};
