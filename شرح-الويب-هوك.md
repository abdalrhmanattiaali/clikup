# 🔗 كيف يعرف النظام أن المهمة اكتملت؟ - شرح Webhook

## 📊 الفكرة الأساسية:

النظام **لا يراقب ClickUp باستمرار**. بدلاً من ذلك:
1. ClickUp **يخبر** النظام عند حدوث أي تغيير
2. يرسل ClickUp طلب HTTP (webhook) للسيرفر
3. النظام يستقبل الطلب ويتصرف بناءً عليه

---

## 🔄 سير العمل الكامل:

```
┌─────────────────┐
│   أنت في ClickUp   │
│  تكمل مهمة ✅    │
└────────┬────────┘
         │
         │ ClickUp يرسل webhook
         ▼
┌─────────────────────────────┐
│  السيرفر (index.js)         │
│  POST /task-updated-webhook │
└────────┬────────────────────┘
         │
         │ يفحص: هل الحالة = "complete"؟
         ▼
    ┌────────┐
    │  نعم   │
    └───┬────┘
        │
        ▼
┌───────────────────────┐
│ يرسل رسالة WhatsApp   │
│ "✅ مهام مكتملة"     │
└───────────────────────┘
```

---

## 📍 Webhook Endpoints الموجودة:

في ملف `index.js` يوجد **3 webhooks**:

### 1. `/task-created-webhook` (السطر 1242)
**متى يُستدعى:** عند **إنشاء** مهمة جديدة
```javascript
app.post('/task-created-webhook', async (req, res) => {
    // يستقبل معلومات المهمة الجديدة
    // يرسل إشعار بإنشاء المهمة
});
```

### 2. `/task-updated-webhook` (السطر 1269) ⭐ **هذا المهم!**
**متى يُستدعى:** عند **تحديث** مهمة (تغيير الحالة، المسؤول، إلخ)
```javascript
app.post('/task-updated-webhook', async (req, res) => {
    // يستقبل التحديث
    // يفحص: هل التحديث هو تغيير الحالة؟
    if (historyItem.field === 'status') {
        // يفحص: هل الحالة الجديدة = مكتمل؟
        if (isComplete) {
            // ✅ يرسل إشعار إنجاز
        }
    }
});
```

### 3. `/task-comment-webhook` (السطر 1336)
**متى يُستدعى:** عند إضافة **تعليق** على مهمة

---

## 🔍 داخل `/task-updated-webhook` بالتفصيل:

### الخطوة 1: استقبال البيانات من ClickUp
```javascript
const body = JSON.parse(req.body.toString('utf8'));
const taskId = body.task_id;
const historyItem = body.history_items?.[0]; // معلومات التحديث
```

**مثال على ما يرسله ClickUp:**
```json
{
  "event": "taskUpdated",
  "task_id": "abc123",
  "history_items": [
    {
      "field": "status",           ← التحديث كان على الحالة
      "before": {
        "status": "In Progress"    ← كانت قيد التنفيذ
      },
      "after": {
        "status": "Complete"       ← أصبحت مكتملة
      },
      "user": {
        "username": "احمد"         ← من غيّر الحالة
      }
    }
  ]
}
```

### الخطوة 2: جلب تفاصيل المهمة الكاملة
```javascript
const task = await getTaskDetails(taskId);
```

يستدعي ClickUp API مرة أخرى للحصول على معلومات كاملة:
- اسم المهمة
- الوصف
- المسؤولين
- المهمة الرئيسية (إذا كانت فرعية)

### الخطوة 3: فحص نوع التحديث
```javascript
switch (historyItem.field) {
    case 'assignee':  // تم تغيير المسؤول
        // ...
        break;

    case 'status':    // ⭐ تم تغيير الحالة
        // هنا نتعامل مع إكمال المهمة
        break;
}
```

### الخطوة 4: فحص إذا كانت الحالة الجديدة = مكتمل
```javascript
case 'status': {
    const afterStatus = task.status?.status;  // "COMPLETE"

    // تسجيل للتشخيص
    console.log('📊 Status Change Detected:');
    console.log('   After:', afterStatus);              // COMPLETE
    console.log('   After (lowercase):', afterStatus.toLowerCase()); // complete

    // الفحص الأساسي
    const isComplete = NON_OPEN_STATUSES.includes(
        afterStatus.toLowerCase().trim()
    );

    if (isComplete) {
        // ✅ المهمة مكتملة!
        await createTaskCompletionNotification(task, updaterName);
    }
}
```

### الخطوة 5: إرسال الإشعار
```javascript
async function createTaskCompletionNotification(task, updaterName) {
    // 1. حفظ البيانات للإحصائيات
    await saveProductivityData({
        type: 'task_completed',
        taskId: task.id,
        taskName: task.name,
        userId: updaterName,
        timestamp: Date.now()
    });

    // 2. إضافة الإشعار للقائمة
    addNotificationToQueue({
        type: 'task_completed',
        task: task,
        data: { updaterName: updaterName }
    });

    // 3. القائمة ترسل للواتساب كل دقيقة
}
```

---

## ⚙️ إعداد Webhook في ClickUp:

### الخطوات:

1. **اذهب إلى ClickUp:**
   - Settings (⚙️)
   - Integrations
   - Webhooks

2. **أنشئ Webhook جديد:**
   - اضغط "Create Webhook"

3. **املأ البيانات:**

   **Workspace:** اختر workspace الخاص بك

   **Endpoint URL:**
   ```
   http://[عنوان-السيرفر]:5014/task-updated-webhook
   ```

   مثال:
   ```
   http://192.168.1.100:5014/task-updated-webhook
   ```

   أو إذا كان السيرفر على الإنترنت:
   ```
   http://yourserver.com:5014/task-updated-webhook
   ```

   **Events to trigger:** اختر:
   - ✅ `taskUpdated` (مهم جداً!)
   - ✅ `taskCreated` (اختياري - للإشعار عند إنشاء مهمة)

4. **احفظ الـ Webhook**

---

## 🧪 اختبار الـ Webhook:

### الطريقة 1: من ClickUp مباشرة

1. افتح مهمة في ClickUp
2. غيّر الحالة من "In Progress" إلى "Complete"
3. راقب السجلات:
```bash
pm2 logs clikup-bot --lines 50
```

**يجب أن ترى:**
```
📊 Status Change Detected:
   Before: In Progress
   After: COMPLETE
   Is Complete?: true
✅ Task marked as COMPLETE - sending notification
📤 Message sent to WhatsApp
```

### الطريقة 2: اختبار يدوي بـ curl

```bash
curl -X POST http://localhost:5014/task-updated-webhook \
  -H "Content-Type: application/json" \
  -d '{
    "event": "taskUpdated",
    "task_id": "test123",
    "history_items": [{
      "field": "status",
      "before": {"status": "In Progress"},
      "after": {"status": "Complete"},
      "user": {"username": "احمد"}
    }]
  }'
```

⚠️ **ملاحظة:** هذا اختبار فقط، لن يعمل لأن `task_id: test123` غير موجود

---

## ❓ الأسئلة الشائعة:

### س: هل يجب أن يكون السيرفر متاح على الإنترنت؟

**ج:** نعم! ClickUp يحتاج للوصول للسيرفر عبر HTTP.

**الخيارات:**
1. **سيرفر على الإنترنت** (VPS, Cloud)
   - URL: `http://your-server.com:5014/task-updated-webhook`

2. **Localhost + Tunnel** (للتطوير فقط)
   - استخدم ngrok أو localtunnel
   ```bash
   npx localtunnel --port 5014
   ```
   - URL: `https://something.loca.lt/task-updated-webhook`

3. **شبكة محلية** (إذا ClickUp والسيرفر على نفس الشبكة)
   - URL: `http://192.168.x.x:5014/task-updated-webhook`

---

### س: لماذا يوجد 3 webhooks مختلفة؟

**ج:** كل واحد له وظيفة:

| Webhook | متى يُستخدم | الوظيفة |
|---------|-------------|---------|
| `/task-created-webhook` | عند إنشاء مهمة | إشعار بمهمة جديدة |
| `/task-updated-webhook` | عند تحديث مهمة | **الأهم** - يكتشف الإنجاز |
| `/task-comment-webhook` | عند إضافة تعليق | إشعار بالتعليقات |

**للاستخدام الأساسي:** فقط `/task-updated-webhook` كافي

---

### س: كيف يعرف النظام أن الحالة "COMPLETE"؟

**ج:** من خلال المقارنة مع قائمة `NON_OPEN_STATUSES`:

```javascript
// قائمة الحالات المكتملة
const NON_OPEN_STATUSES = [
    'complete',    // ← يقارن هنا
    'completed',
    'done',
    // ...
];

// الفحص
const afterStatus = "COMPLETE";  // من ClickUp
const isComplete = NON_OPEN_STATUSES.includes(
    afterStatus.toLowerCase()    // "COMPLETE" → "complete" ✓
);
```

---

### س: لماذا لا تصلني إشعارات؟

**تحقق من:**

1. **Webhook مُعد في ClickUp؟**
   ```bash
   # في ClickUp → Settings → Integrations → Webhooks
   # تأكد من وجود webhook نشط
   ```

2. **السيرفر شغال ويستقبل طلبات؟**
   ```bash
   curl http://localhost:5014/api/dashboard-stats
   # يجب أن تعمل
   ```

3. **Webhook URL صحيح؟**
   ```
   http://[السيرفر]:5014/task-updated-webhook
                           ^^^^^^^^^^^^^^^^^^^
                           تأكد من الاسم الصحيح!
   ```

4. **الحالة في القائمة؟**
   ```bash
   curl http://localhost:5014/debug/status-check/COMPLETE
   # يجب: "isComplete": true
   ```

5. **WhatsApp متصل؟**
   ```bash
   pm2 logs clikup-bot | grep "WhatsApp Ready"
   ```

---

## 🎯 ملخص سريع:

```
ClickUp (تكمل مهمة)
    ↓
يرسل webhook POST إلى:
    http://السيرفر:5014/task-updated-webhook
    ↓
index.js يستقبل الطلب
    ↓
يفحص: historyItem.field === 'status'?
    ↓
يفحص: isComplete?
    ↓
يحفظ البيانات في productivity_data.json
    ↓
يرسل إشعار للواتساب ✅
```

---

## 📝 الخطوات التالية:

1. ✅ تأكد من إعداد Webhook في ClickUp على:
   ```
   http://[السيرفر]:5014/task-updated-webhook
   ```

2. ✅ تأكد من Events تحتوي على `taskUpdated`

3. ✅ أكمل مهمة وراقب السجلات

4. ✅ إذا لم تعمل، اتبع دليل `تشخيص-حالة-المهام.md`

---

**🎉 الآن فهمت بالضبط كيف يعرف النظام أن المهمة اكتملت!**
