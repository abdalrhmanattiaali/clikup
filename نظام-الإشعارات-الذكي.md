# 🤖 نظام الإشعارات الذكي بالـ AI

## 🎯 الفكرة الأساسية

نظام جديد يستخدم **Claude AI** لفهم أي تغيير في المهام وكتابة إشعارات ذكية بالعربية تلقائياً!

---

## ✨ ماذا تغيّر؟

### قبل (النظام القديم):
- يرسل إشعار فقط عند **إنجاز** المهام
- رسائل ثابتة مكررة
- لا يفهم سياق المهمة

### الآن (النظام الجديد):
- ✅ يرسل إشعار عند **أي تغيير** في المهمة
- ✅ AI يفهم السياق ويكتب رسالة مناسبة
- ✅ يدعم جميع أنواع التغييرات:
  - تغيير الحالة (Status)
  - تعيين/إزالة مسؤول (Assignee)
  - تغيير الأولوية (Priority)
  - تحديد موعد نهائي (Due Date)
  - تعديل الوصف (Description)
  - تغيير العنوان (Name)
  - أي تحديث آخر

---

## 🔄 كيف يعمل النظام؟

### 1. ClickUp يرسل Webhook
عند أي تغيير في مهمة:
```json
{
  "event": "taskUpdated",
  "task_id": "abc123",
  "history_items": [{
    "field": "status",
    "before": {"status": "In Progress"},
    "after": {"status": "Complete"},
    "user": {"username": "محمد"}
  }]
}
```

### 2. النظام يستقبل ويحلل
```javascript
// index.js - يستقبل الـ webhook
app.post('/task-updated-webhook', async (req, res) => {
    // يستخرج التفاصيل
    const field = historyItem.field;      // "status"
    const before = historyItem.before;    // "In Progress"
    const after = historyItem.after;      // "Complete"
    const userName = historyItem.user.username; // "محمد"

    // يجلب تفاصيل المهمة من ClickUp API
    const task = await getTaskDetails(taskId);
});
```

### 3. يرسل للـ AI مع السياق الكامل
```javascript
// ai-notifications.js - توليد الإشعار
const aiNotification = await generateSmartNotification(task, change, parentTask);
```

### 4. AI يفهم ويكتب الإشعار
Claude AI يحلل:
- ✅ نوع التغيير
- ✅ اسم المهمة
- ✅ الشخص الذي قام بالتغيير
- ✅ هل هي مهمة فرعية؟
- ✅ ما الأولوية؟
- ✅ ما السياق المناسب؟

ثم يكتب إشعار مناسب بالعربية!

### 5. إرسال للواتساب
```javascript
// يرسل الإشعار للمجموعة
await cleanAndSendMessage(GROUP_CHAT_ID, aiNotification);
```

---

## 📝 أمثلة على الإشعارات الذكية

### مثال 1: إنجاز مهمة
**التغيير:** Status من "In Progress" إلى "Complete"

**الإشعار المولّد:**
```
✅ رائع! أنجز محمد مهمة "تصميم الواجهة الرئيسية"
المهمة الفرعية من: تطوير تطبيق الموبايل
أحسنت يا محمد! 🎉
```

### مثال 2: تغيير حالة عادية
**التغيير:** Status من "To Do" إلى "In Progress"

**الإشعار المولّد:**
```
🔄 بدأ أحمد العمل على مهمة "مراجعة الكود"
الحالة الجديدة: قيد التنفيذ
بالتوفيق! 💪
```

### مثال 3: تعيين مهمة جديدة
**التغيير:** Assignee تم تعيين "سارة"

**الإشعار المولّد:**
```
📋 تم تعيين مهمة "كتابة التوثيق" لـ سارة
الأولوية: عاجلة 🔴
الموعد النهائي: خلال 3 أيام
بالتوفيق يا سارة! 🌟
```

### مثال 4: تغيير الأولوية
**التغيير:** Priority من "عادية" إلى "عاجلة"

**الإشعار المولّد:**
```
⚡ رفع علي أولوية مهمة "إصلاح الخطأ الحرج" إلى عاجلة 🔴
المهمة تحتاج انتباه فوري!
```

### مثال 5: تحديد موعد نهائي
**التغيير:** Due Date تم إضافته

**الإشعار المولّد:**
```
⏰ حدد خالد موعد نهائي لمهمة "إعداد التقرير الشهري"
الموعد: خلال يومين
لا تنسوا! 📅
```

---

## 🎨 ما يجعل النظام ذكياً؟

### 1. فهم السياق
```javascript
// AI يحصل على:
- اسم المهمة: "تصميم الواجهة الرئيسية"
- الوصف: "تصميم واجهة جديدة للصفحة الرئيسية"
- المسؤولون: ["محمد", "سارة"]
- الأولوية: عاجلة
- المهمة الرئيسية: "تطوير تطبيق الموبايل"
- التغيير: من "In Progress" إلى "Complete"
- الشخص: محمد
```

### 2. اختيار Emoji مناسب
- ✅ للإنجاز
- 🔄 لتغيير الحالة
- 📋 للتعيين
- ⚡ للأولوية
- ⏰ للمواعيد
- 🎯 للأهداف

### 3. لغة ودودة
AI يستخدم لغة:
- ✅ عربية فصيحة وبسيطة
- ✅ ودودة ومشجعة
- ✅ مختصرة (2-4 أسطر)
- ✅ واضحة ومباشرة

### 4. تشجيع الفريق
```
أحسنت!
استمروا في العمل الرائع!
بالتوفيق!
رائع! 🎉
```

---

## 🔧 الملفات المعنية

### 1. `ai-notifications.js` (جديد)
نظام AI الأساسي:
```javascript
async function generateSmartNotification(task, change, parentTask)
async function generateCompletionNotification(task, userName, parentTask)
async function generateAssignmentNotification(task, assignee, assigner, parentTask)
```

### 2. `index.js` (تم التحديث)
Webhook handler الجديد:
```javascript
app.post('/task-updated-webhook', async (req, res) => {
    // يكتشف نوع التغيير
    const change = {
        field: historyItem.field,
        before: beforeValue,
        after: afterValue,
        userName: updaterName
    };

    // يرسل لـ AI
    await createAISmartNotification(task, change);
});
```

### 3. `.env`
مفتاح API للـ AI:
```env
ANTHROPIC_API_KEY=sk-ant-api03-your-key-here
```

---

## 🎯 أنواع التحديثات المدعومة

| النوع | الحقل | مثال على الإشعار |
|-------|-------|-------------------|
| ✅ تغيير الحالة | `status` | "🔄 نقل أحمد المهمة إلى قيد المراجعة" |
| 👤 تعيين مسؤول | `assignee` | "📋 تم تعيين المهمة لـ سارة" |
| ⚡ تغيير الأولوية | `priority` | "⚡ رفعت الأولوية إلى عاجلة جداً" |
| 📅 موعد نهائي | `due_date` | "⏰ الموعد النهائي: خلال 3 أيام" |
| 📝 تعديل الوصف | `description` | "📝 تم تحديث وصف المهمة" |
| ✏️ تغيير العنوان | `name` | "✏️ تم تغيير عنوان المهمة" |
| 🔔 تحديثات أخرى | `*` | "🔔 تم تحديث المهمة" |

---

## ⚙️ كيف تشغّل النظام؟

### 1. تأكد من مفتاح AI في `.env`
```env
ANTHROPIC_API_KEY=sk-ant-api03-your-actual-key-here
```

### 2. ثبّت التبعيات (إذا لم تكن مثبتة)
```bash
npm install @anthropic-ai/sdk
```

### 3. أعد تشغيل النظام
```bash
pm2 restart clikup-bot
```

### 4. راقب السجلات
```bash
pm2 logs clikup-bot --follow
```

### 5. جرّب تغيير أي مهمة في ClickUp!
- غيّر الحالة
- عيّن مسؤول
- غيّر الأولوية
- أضف موعد نهائي

---

## 📊 ما ستراه في السجلات

```bash
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📊 Task Update Detected
   Task: تصميم الواجهة الرئيسية
   Task ID: abc123
   Changed by: محمد
   Field changed: status
   Before: In Progress
   After: Complete
🤖 Sending to AI for smart notification generation...
🤖 AI: Generating smart notification...
   Task: تصميم الواجهة الرئيسية
   Change: status
   By: محمد
✅ AI: Notification generated
   Message: ✅ رائع! أنجز محمد مهمة "تصميم الواجهة ا...
✅ AI notification sent successfully
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

---

## 🔍 استكشاف الأخطاء

### المشكلة 1: لا تصل إشعارات

**تحقق من:**
1. مفتاح AI صحيح في `.env`
   ```bash
   grep ANTHROPIC_API_KEY .env
   ```

2. النظام يعمل
   ```bash
   pm2 status
   ```

3. Webhook مُعد في ClickUp
   ```
   http://147.93.94.125:5014/task-updated-webhook
   ```

4. راقب السجلات عند تغيير مهمة
   ```bash
   pm2 logs clikup-bot --follow
   ```

### المشكلة 2: خطأ في AI

إذا فشل AI، النظام يستخدم **fallback** تلقائياً:
```javascript
// يكتب إشعار بسيط بدون AI
return generateFallbackNotification(task, change, parentTask);
```

### المشكلة 3: رسائل AI غير مناسبة

يمكنك تعديل prompt الـ AI في:
```javascript
// ai-notifications.js - السطر 80+
function buildTaskContext(task, change, parentTask) {
    // عدّل التعليمات هنا
}
```

---

## 🎁 مزايا إضافية

### 1. يحفظ الإحصائيات
```javascript
await saveProductivityData({
    type: `task_${change.field}_changed`,
    taskId: task.id,
    field: change.field,
    before: change.before,
    after: change.after,
    timestamp: Date.now()
});
```

### 2. يدعم المهام الفرعية
```javascript
// إذا كانت مهمة فرعية، يذكر المهمة الرئيسية
if (parentTask) {
    notification += `\nالمهمة الفرعية من: ${parentTask.name}`;
}
```

### 3. يرسل إشعار مباشر للمسؤول
```javascript
// عند تعيين مسؤول جديد
if (change.field === 'assignee' && change.after) {
    await sendDirectAssignmentNotification(task);
}
```

---

## 📈 إحصائيات

يحفظ بيانات كل تغيير في:
```
productivity_data.json
```

مثال:
```json
[
  {
    "type": "task_status_changed",
    "taskId": "abc123",
    "taskName": "تصميم الواجهة",
    "userId": "محمد",
    "timestamp": 1234567890,
    "field": "status",
    "before": "In Progress",
    "after": "Complete"
  }
]
```

---

## 🚀 الميزات المستقبلية

### قيد التطوير:
- [ ] تلخيص يومي لجميع التحديثات
- [ ] اكتشاف الأنماط (مثلاً: مهمة متأخرة)
- [ ] اقتراحات ذكية للفريق
- [ ] رسوم بيانية للتحديثات

---

## 💡 نصائح للاستخدام الأمثل

### 1. استخدم أسماء واضحة للمهام
AI يفهم أفضل مع أسماء وصفية:
- ✅ "تصميم واجهة الصفحة الرئيسية"
- ❌ "المهمة 1"

### 2. أضف وصف للمهام المهمة
AI يستخدم الوصف للسياق الأفضل

### 3. استخدم الأولويات
AI يذكر الأولوية في الإشعار

### 4. ربط المهام الفرعية بالرئيسية
AI يذكر المهمة الرئيسية تلقائياً

---

## 🎯 الخلاصة

### قبل:
```
✅ مهام مكتملة (1):
- تصميم الواجهة ✓ بواسطة: محمد
```

### الآن:
```
✅ رائع! أنجز محمد مهمة "تصميم الواجهة الرئيسية"
المهمة الفرعية من: تطوير تطبيق الموبايل
الأولوية: عاجلة 🔴
أحسنت يا محمد! 🎉
```

**الفرق:** AI يفهم السياق ويكتب إشعار مخصص ومناسب! 🤖✨

---

## 📞 الدعم

إذا واجهت مشاكل:
1. راجع `استكشاف الأخطاء` في هذا الملف
2. شغّل `./diagnose-webhook.sh`
3. تحقق من السجلات: `pm2 logs clikup-bot`

---

**🎉 استمتع بإشعارات ذكية ومخصصة لكل تغيير في مهامك!**
